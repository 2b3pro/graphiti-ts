# v0.2.0 Release Checklist

## Feature Matrix

| Group | Item | Status | Notes |
|---|---|---|---|
| A | `EpisodeTypes.document` | Present | Added in [`packages/core/src/domain/nodes.ts`](/Volumes/Exagora/Projects/graphiti-ts/packages/core/src/domain/nodes.ts) |
| A | `custom_extraction_instructions` in node prompts | Present | Wired and tested |
| A | `custom_extraction_instructions` in edge prompts | Present | Wired and tested |
| B | GDS Leiden community detection | Present | Runtime auto-detected |
| B | Batched community summarization/naming | Present | Source already contained batched prompt path |
| C | Staleness, negation, lifecycle APIs | Present | Source already contained lifecycle modules and APIs |
| C | Conditional edges and anchoring | Present | Exported additive edge fields |
| D | Confidence, epistemic status, searchAsOf, Jina reranker | Present | Additive public surface |
| D | Full-ingestion persistence correctness | Corrected | Bulk write path now uses canonical serialization |
| D | FalkorDB scoped routing correctness | Corrected | Driver resolution is now operation-scoped |
| D | Community rebuild scoping | Corrected | Cleanup is filtered by `group_id` |
| D | Group deletion completeness | Corrected | Cleanup now removes all group-owned graph records by `group_id` |
| D | Deprecation metadata round-trip | Corrected | Persisted/read as top-level edge fields |
| D | Typed policy config | Present | `Graphiti({ config })` now supports extraction defaults, community strategy, bulk embedding preference, and deprecation-gate tuning |

## Verification Gates

| Gate | Status | Notes |
|---|---|---|
| Targeted core regressions | Complete | `graphiti.test.ts`, `bulk-utils.test.ts`, prompt and node-operation tests pass |
| `@graphiti/core` typecheck | Complete | Passes after refactor and test updates |
| Full workspace build | Complete | `bun run build` passes |
| Full workspace typecheck | Complete | `bun run typecheck` passes |
| Full workspace test | Complete | `bun run test` passes; DB integration suites remain intentionally skipped |

## Release Notes

- `dist/` should be rebuilt once at the release cut, not during normal source iteration.
- Policy-level config is now available through `Graphiti({ config })`; document ingestion remains an additive API feature.
- Remaining release work is packaging verification, not core stabilization.
